<!DOCTYPE html>
<html>
<head>
  <title>My Reverie Photography</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="user_nav">
    <% if current_user %>
      <span>Signed in as <%= current_user.username %></span>&nbsp;&nbsp;&nbsp;
      <%= link_to "Edit profile", edit_user_path(:current) %>&nbsp;&nbsp;&nbsp;
      <%= link_to "Logout", logout_path %>
    <% else %>
      <%= link_to "Log in", login_path %> 
    <% end %>
  </div>
  <div id="container">
    <div id="header">
      <h1><a href="/">MY REVERIE PHOTOGRAPHY</a></h1>
    </div>
    <%= yield %>
  </div>
  <div id="footer">
    <p>POWERED BY GLOB - <a href="http://www.github.com/kbeswick/glob">@github</a></p>
  </div>
</body>
</html>
